I’m building a production-grade Windows 11 AI-augmented developer workstation centered on Claude Code — a deterministic “workshop laptop” setup that I can provision from scratch and trust in daily use. This isn’t a pile of installs; it’s an engineered system: Windows 11 + WSL2, PowerShell 7, Windows Terminal, VS Code, Git (plus Git Bash where it’s the right tool), Claude Code configured with repo-scoped memory (CLAUDE.md / .claude/settings.json), MCP servers, strict safety guardrails, and real observability (token/cost/model/status line). I want it to feel like an internal platform team built it.

Architect it so the environment is modular but opinionated. I need swappable pieces (shell choice, MCP servers, IDE, model routing, tooling), but I refuse a fragile plugin circus. It must be extensible yet focused, robust yet lightweight, configurable yet shipped with strong defaults. Make every destructive action hard to do accidentally (both at the Claude permission layer and at the shell layer), while still allowing autonomous flows like: clone repo → bootstrap context → implement feature → run tests → commit → open PR. No global-only magic that breaks on a new machine. No context rot. Repo-level truth wins.

I want you to design it as a layered architecture, and I need you to explain your own decision process as you do it. Describe YOUR process for deciding what belongs at OS level vs user profile vs repo level. Describe YOUR approach to preventing context rot across long-running sessions and across multiple repositories. Describe YOUR strategy for safety: what gets blocked, where, and why (Claude permissions, pre-tool hooks, shell aliases, git protections). Describe YOUR observability model: what you track (model, cost, tokens, cache hits if available, git branch, task state), how it’s surfaced (status line), and how you keep it from cluttering or confusing CLI parsing.

The system needs to cover these surfaces concretely, with strong defaults and clean extension points:

* Windows provisioning: Winget (prefer DSC-style declarative provisioning if viable), enabling Developer Mode, WSL2 install + Ubuntu (or a sensible default), Git, Node/Python toolchains, Docker Desktop (if recommended), VS Code, Windows Terminal, and any CLI dependencies for Claude Code and MCP.
* Terminal substrate: Windows Terminal profile(s) that launch into the right shell and default directory, with fonts/rendering that won’t break Unicode UI for agentic CLIs, and a prompt that’s minimal/deterministic to reduce misreads.
* PowerShell 7: a $PROFILE that prioritizes reliability and speed (history/search/editing, directory jumping, git ergonomics), but doesn’t spam output or introduce nondeterministic prompt artifacts that confuse agents.
* Git Bash (when used): path correctness, login-shell sourcing, alias expansion, and winpty wrapping for Windows binaries so interactive CLIs behave. Make it stable in VS Code integrated terminal too.
* VS Code ergonomics: settings.json and keybindings.json tuned for an AI workflow (format-on-save, terminal focus toggles, task execution shortcuts, remote/WSL keybinding gotchas). Keep it “fast to supervise agents.”
* Claude Code: settings hierarchy (org/system/global/project/local), repo-scoped CLAUDE.md generation conventions, .claude/settings.json with explicit permissions/deny rules, and a status line command that surfaces real metrics without noise.
* MCP: include a practical MCP set for “serious dev” (doc fetch / context retrieval, code intelligence/LSP, task management, provider routing if relevant). Define how to configure and validate MCP servers, plus a failure plan when MCP is down.
* Multi-repo + multi-agent: strategies for working across many services without context pollution, and concurrency via worktrees so multiple agents can operate safely in parallel.

Now here’s the deliverable constraint stack: give me something I can execute, but that still reads like an architecture doc a senior engineer respects. It has to be compact but comprehensive, elegant but explicit, readable yet information-dense. Every file/config snippet must earn its existence. No tutorial tone. Assume I know what WSL and VS Code are — don’t explain basics. Use your full capability here; go deep.

Output it in four representations that descend from strategic to executable:

1. A high-level manifesto that states invariants and design philosophy (what must always be true about this environment).
2. A textual systems diagram showing boundaries and control/data flow (OS → shells → terminal → IDE → Claude → MCP → repo memory → git workflow).
3. A stepwise bootstrapping blueprint: the exact sequence to go from fresh Windows 11 to ready-to-code (Winget provisioning + shell profile + terminal profiles + VS Code sync + repo template + Claude init + MCP validation). Include “verification steps” after each major phase.
4. Concrete artifacts: sample Windows Terminal settings fragments, PowerShell $PROFILE snippet, Git Bash .bash_profile/.bashrc snippets (including winpty), VS Code settings/keybindings snippets, .claude/settings.json template (with safe permissions), CLAUDE.md template, and a skeleton bootstrap script outline that ties it together.

Template/generalize it: parameterize what changes for macOS, Linux bare metal, devcontainers, and remote WSL/SSH. Also parameterize what changes for Cursor/Copilot/future agents while keeping the invariant layers intact. Seed 2–3 future domains where this same approach should work (e.g., data engineering notebooks, infra-as-code repos, security auditing workflows) and show which knobs get turned.

Finally, make this prompt self-aware: analyze what makes your proposed architecture resilient (and where it fails). Identify the hidden fragilities (agent hallucinations in terminals, prompt injection in repos, nondeterministic shell output, multi-agent file conflicts). Propose the next iteration (v2) improvements you’d make after real-world use. Then tell me what I’m not thinking about and add whatever you’d want if you were building this as your own daily driver.
